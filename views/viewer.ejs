<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Watchlist</title>
    <link rel="icon" href="/images/favicon.ico">
    <script src="/js/viewer.js"></script>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
    <header class="masthead mb-auto">
        <nav class="navbar navbar-expand-lg navbar-dark permanent-marker-regular">
          <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <img src = "/images/favicon.ico" width="30" height="30" class="d-inline-block align-top" alt="">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>  
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page"  href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/watchlist">Watchlist</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/randomize">Randomizer</a>
                    </li>
                </ul>
                <span class="navbar-text d-flex align-items-center">
                <% if (username) { %>
                    <span class="me-2">[ <%= username %> ]</span>
                    <form action="/auth/logout" method="post" class="d-inline">
                        <button class="btn btn-outline-danger btn-sm" type="submit" name="Logout" value="Logout">Logout</button>
                    </form>
                    <% } else { %>
                    <a href="/auth/login" class="btn btn-primary btn-sm">Log in / Sign up</a>
                    <% } %>
              </span>
            </div>
          </div>
        </nav>
    </header>
    <main class="inner cover container" role="main">
        <h1>Your Watchlist</h1>

        <h2>Movies</h2>
        <ul>
            <% if (movies && movies.length > 0) { %>
                <% movies.forEach(function(movie) { %>
                    <li>
                        <%= movie.title %> (<%= movie.year %>)
                        <img src="<%= movie.poster || '/images/doc_marty.jpg' %>" alt="<%= movie.title %>" width="100"> -->
                        <button class="remove-button" id="remove-button" data-id="<%= movie.id %>" data-type="movie">Remove</button>
                    </li>
                <% }); %>
            <% } else { %>
                <p>No movies in your watchlist.</p>
            <% } %>
        </ul>

        <h2>TV Shows</h2>
        <ul>
            <% if (tvShows && tvShows.length > 0) { %>
                <% tvShows.forEach(function(tvShow) { %>
                    <li>
                        <%= tvShow.title %> (<%= tvShow.year %>)
                        <img src="<%= tvShow.poster || '/images/doc_marty.jpg' %>" alt="<%= tvShow.title %>" width="100"> -->                   
                        <button class="remove-button" id="remove-button" data-id="<%= tvShow.id %>" data-type="tv">Remove</button>
                    </li>
                <% }); %>
            <% } else { %>
                <p>No TV shows in your watchlist.</p>
            <% } %>
        </ul>
    </main>
    <footer class="footer">
        <div class = "container">
            <span class="permanent-marker-regular">Created by Damien Barrett for NCIRL College Project 2024</span>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>  
</body>
</html>

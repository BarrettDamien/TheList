<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Watchlist</title>
    <link rel="icon" href="/images/favicon.ico">
    <script src="/js/watchlist.js" defer></script>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
    <body>
        <header class="masthead mb-auto">
            <nav class="navbar navbar-expand-lg navbar-dark permanent-marker-regular">
              <div class="container-fluid">
                <a class="navbar-brand" href="/">
                    <img src = "/images/favicon.ico" width="30" height="30" class="d-inline-block align-top" alt="">
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>  
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page"  href="/">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link active" href="/watchlist">Watchlist</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/randomize">Randomizer</a>
                        </li>
                    </ul>
                    <span class="navbar-text d-flex align-items-center">
                    <% if (username) { %>
                        <span class="me-2">[ <%= username %> ]</span>
                        <form action="/auth/logout" method="post" class="d-inline">
                            <button class="btn btn-outline-danger btn-sm" type="submit" name="Logout" value="Logout">Logout</button>
                        </form>
                        <% } else { %>
                        <a href="/auth/login" class="btn btn-primary btn-sm">Log in / Sign up</a>
                        <% } %>
                  </span>
                </div>
              </div>
            </nav>
        </header>
        <main class="inner cover container" role="main">
            <% if (user) { %>
                <p hidden>Welcome, <%= user.username %>!</p>
                <p hidden>Your User ID is: <%= user.id %></p>
                <script>
                    const userId = '<%= JSON.stringify(user.id) %>'; // Embed the user ID from the server-side EJS rendering
                    console.log("User ID:", userId);
                </script>
            <% } else { %>
                <p>Please log in to see your watchlist.</p>
            <% } %>

            <!-- Pills navs -->
            <ul class="nav nav-fill nav-tabs" id="tabs" role="tablist">
                <li class="nav-item" role="presentation">
                    <a class="nav-link active" id="movie-tab" data-bs-toggle="tab" href="#movie-tabpanel" role="tab" aria-controls="movie-tabpanel" aria-selected="true">Movies</a>
                </li>
                <li class="nav-item" role="presentation">
                    <a class="nav-link" id="tv-tab" data-bs-toggle="tab" href="#tv-tabpanel" role="tab" aria-controls="tv-tabpanel" aria-selected="false">TV Shows</a>
                </li>
            </ul>
            <!-- Pills navs -->

            <!-- Pills content -->
            <div class="tab-content pt-5" id="tab-content">
                <div class="tab-pane active" id="movie-tabpanel" role="tabpanel" aria-labelledby="movie-tab">
                    <h3 hidden>Search for Movies</h3>
                    <input type="text" id="movie-search-input" placeholder="Enter movie title">
                    <button id="movie-search-button" class="btn btn-primary">Search</button>
                    <div id="movie-search-results" class="mt-4"></div>
                    <nav aria-label="Movie results pagination">
                        <ul class="pagination justify-content-center mt-3" id="movie-pagination"></ul>
                    </nav>
                </div>
        
                <div class="tab-pane" id="tv-tabpanel" role="tabpanel" aria-labelledby="tv-tab">
                    <h3 hidden>Search for TV Shows</h3>
                    <input type="text" id="tv-search-input" placeholder="Enter TV show title">
                    <button id="tv-search-button" class="btn btn-primary">Search</button>
                    <div id="tv-search-results" class="mt-4"></div>
                    <nav aria-label="TV show results pagination">
                        <ul class="pagination justify-content-center mt-3" id="tv-pagination"></ul>
                    </nav>
                </div>
            </div>
            <!-- Pills content -->

            <a href="/watchlist/viewer">Go to Viewer</a>

            <h1 hidden>Add API References below</h1>
        </main>
        <footer class="footer">
            <div class = "container">
                <span class="permanent-marker-regular">Created by Damien Barrett for NCIRL College Project 2024</span>
            </div>
        </footer>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>         
    </body>
</html>